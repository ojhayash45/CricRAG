# --- Core app / config -----------------------------------------------
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
python-dotenv>=1.0,<2

# --- Ingestion (Phase 1-3) ---------------------------------------------
beautifulsoup4>=4.12,<5
lxml>=5.1,<6
pypdf>=4.0,<6

# --- Chunking / embeddings / vector search (Phase 4-6) -----------------
# fastembed (ONNX Runtime) instead of sentence-transformers (PyTorch) --
# same default model, far smaller memory footprint. See
# src/embeddings/embedding_service.py for why.
fastembed>=0.3,<1
faiss-cpu>=1.8,<2
# fastembed requires numpy>=2.1 on Python 3.13+ (>=2.3 on 3.14+); pinning
# numpy<2 here caused an unresolvable conflict on Render's build (Python
# 3.13). Let pip pick whichever numpy 1.x/2.x satisfies every constraint.
numpy>=1.26,<3

# --- LLM generation (Phase 8) -------------------------------------------
openai>=1.0,<2

# --- UI (Phase 10) --------------------------------------------------------
streamlit>=1.33,<2

# --- Testing --------------------------------------------------------------
pytest>=8.0,<9
