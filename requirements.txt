# --- Core app / config -----------------------------------------------
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
python-dotenv>=1.0,<2

# --- Ingestion (Phase 1-3) ---------------------------------------------
beautifulsoup4>=4.12,<5
lxml>=5.1,<6
pypdf>=4.0,<6

# --- Chunking / embeddings / vector search (Phase 4-6) -----------------
sentence-transformers>=2.7,<3
faiss-cpu>=1.8,<2
numpy>=1.26,<2

# --- LLM generation (Phase 8) -------------------------------------------
openai>=1.0,<2

# --- UI (Phase 10) --------------------------------------------------------
streamlit>=1.33,<2

# --- Testing --------------------------------------------------------------
pytest>=8.0,<9
